# QuantBot — Python Dependencies
# Install all of these with: pip install -r requirements.txt

# Data fetching
requests>=2.31.0          # Makes HTTP requests to APIs
websocket-client>=1.6.0   # Real-time price feeds (WebSocket)
yfinance>=0.2.40          # Free stock/forex/index data (Yahoo Finance)
python-binance>=1.0.19    # Binance crypto API

# Data handling
pandas>=2.2.0             # Tables of price data (DataFrames)
numpy>=2.2.0              # Maths and arrays

# Indicators (alternative to coding from scratch)
pandas-ta>=0.4.67b0       # RSI, ATR, EMA and more — built in

# Visualisation
matplotlib>=3.8.0         # Basic charts
plotly>=5.18.0            # Interactive charts

# Web server (for TradingView webhooks)
flask>=3.0.0

# Environment variables (keeps API keys safe)
python-dotenv>=1.0.0

# Scheduling (run strategy at set times)
schedule>=1.2.1
